import 'package:flutter/material.dart';

class FiTransactionScreen extends StatelessWidget {
  const FiTransactionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("jdcb"),),
    );
  }
}
